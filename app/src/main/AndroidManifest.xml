<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.easydanger.rockshambout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ng"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ng"
        android:supportsRtl="true"

        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".ChooseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DannyActivity"></activity>
        <activity android:name=".BartActivity"></activity>
        <activity android:name=".HannahActivity"></activity>
    </application>

</manifest>